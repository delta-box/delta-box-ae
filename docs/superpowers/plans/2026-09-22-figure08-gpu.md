# Figure 8 GPU and Theory Implementation Plan

> **For agentic workers:** Use subagent-driven-development for the independent theory task; complete specification and code review before publishing. The user has authorized script implementation before GPU allocation.

**Goal:** Deliver runnable GPU timing scripts for Figure 8(b), and a tested CPU calculator/plotter for Figure 8(c).

**Architecture:** Keep GPU measurement separate from the CPU experiment catalog. A stdlib supervisor plans/checks/runs isolated worker cases and stores raw evidence; the independent calculator combines explicit timing inputs using Equation 1. GPU execution remains opt-in until hardware and a local model are configured.

**Tech stack:** Python standard library, optional matplotlib, vLLM for generation, torch/transformers/peft for LoRA and FSDP training.

## Task 1 — CPU model and plots

Files: `ae/repro/gpu_occupation.py`, `ae/repro/figure08_plots.py`, `tests/paper/test_gpu_occupation.py`.

- [x] Implement the exact JSON contracts in the specification. For `(sandbox, gen, train)=(1,1,2)` seconds assert `occupation=.75` and `staleness_versions=1`.
- [x] Reject negative/non-finite/boolean timings, zero training time, duplicate and missing requested points; propagate estimated inputs.
- [x] Import successful suite `cases[*].timing_s.mean` and canonical `figure-08.series` millisecond points. Never infer missing time as zero.
- [x] Add `--input` or `--gpu-results` plus `--fanout-summary`, `--output` and `--plot` CLI. Output directories must be fresh.
- [x] Plot theory and timing suites with matplotlib imported only on request. Label input provenance and estimates.
- [x] Run `python3 -m unittest tests.paper.test_gpu_occupation -v`; prove malformed inputs fail and a no-GPU CLI succeeds.

## Task 2 — GPU recipe, supervisor, and worker

Files: `ae/configs/figure08-gpu.json`, `ae/runners/gpu_timing.py`, `ae/runners/gpu_worker.py`, `tests/paper/test_gpu_timing.py`.

- [x] Validate the eight paper cases: generation B=1/4/16/64 on one GPU; train B=1/4 on one, B=16/64 on four with microbatch 4 and accumulation 1/4.
- [x] `plan` and dependency `check` must not import GPU frameworks. Test missing resources, duplicate devices, invalid repetitions and incompatible batch arithmetic.
- [x] Use a local model, per-case child environment, isolated output/logs and bounded subprocess lifetime. Capture process failure instead of fabricating a result.
- [x] Implement original generation and training timing boundaries, including optimizer/synchronization. Record actual token counts and input protocol; exact-token mode is distinct.
- [x] Validate every successful raw result against its planned phase, batch, devices and repetition count before aggregation.
- [x] Test supervisor success/failure with injected executors and fake worker records. Compile GPU-only branches without executing them.

## Task 3 — Historical inputs and documentation

Files: `ae/configs/figure08-theory-paper.json`, `ae/paper/figure-08/README.md`, `ae/README.md`, `ae/repro/catalog.py`, current skip/summary wording, and `ae/report/figure08-script-preparation-20260922/`.

- [x] Verify selected historical records against the committed object hashes; compute means from raw repetitions.
- [x] Preserve E2B's estimated N64 fanout and record the original plot/benchmark source identities.
- [x] Calculate and render panel (c) on CPU. Keep any generated (b) preview explicitly historical; no fresh GPU timing is claimed.
- [x] Expose panel (c) as CPU-derived and panel (b) as an available, opt-in GPU runner awaiting resources. Keep CPU catalog counts stable.
- [x] Document local model/Python paths, four selected devices, H20 paper baseline, actual memory observations, quick smoke, full matrix, failure logs and theory commands.

## Task 4 — Verification and delivery

- [x] Run focused and existing impacted unit tests; execute no-GPU plans/checks and CPU theory, including NUMA3 checks on spr4numa.
- [x] Independently review specification fidelity, timing boundaries, process cleanup, input provenance and missing-data behavior; fix findings.
- [x] Commit source/config/tests/docs, refresh the source lock after source commit, and verify it.
- [x] Merge/push to GitHub with clear CPU-validated/GPU-pending status. Report exact resource information needed for actual GPU execution.

## Delivery record

GitHub `main` was verified at `5f547aa68a75a2bc953d091aa4a64c280d28aa53` after the local fast-forward merge and push. Frozen executable sources remain `1bc367d` / SHA-256 `ebc46428d8766e67016405266a293a11f87ffefb6114b027c9619fbcef49e04e`: 139/139 CPU checks passed on spr4numa NUMA3, with no skips. All 39 evidence files matched their recorded hashes. GPU execution remains pending allocation.

The two pre-existing root README working-tree edits were preserved byte-for-byte. Exported CSV evidence retains `csv.writer`'s CRLF bytes; the final whitespace check used Git's `cr-at-eol` setting without altering the evidence or its hashes.
